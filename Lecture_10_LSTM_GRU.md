# المحاضرة العاشرة — LSTM & GRU

> Neural Network Course — FCIS, Mansoura University
> Book Pages: 313–338

---

## Long Short-Term Memory (LSTM)

الـ **LSTM** هي نوع خاص من الـ RNN مصمم عشان يحل مشكلة الـ **Vanishing Gradient** ويتعامل مع **long-term dependencies**.

> يعني بدل ما الشبكة تنسى المعلومات بسرعة زي الـ RNN العادي، الـ LSTM عندها ذاكرة أقوى بتحتفظ بالمعلومات لفترة أطول.

### البنية:

```
                    ┌───────────────────────────────────┐
                    │           Cell State (C)          │
                    │  ────────×──────────+──────────── │──→ C(t)
                    │         ↑          ↑              │
                    │    ┌────┴────┐ ┌───┴────┐         │
                    │    │ Forget  │ │ Input  │×──┐     │
                    │    │  Gate   │ │  Gate  │   │     │
x(t) ──┐           │    └────┬────┘ └───┬────┘ ┌─┴──┐  │
       ├──→ ┌──────┤         │          │      │ C̃  │  │
h(t-1)─┘   │ LSTM ├─────────┴──────────┴──────┴────┘  │
            │      │                                    │
            │      │    ┌──────────┐                    │
            │      │    │  Output  │                    │
            │      │    │   Gate   │                    │
            └──────┤    └────┬─────┘                    │
                    │         │                          │
                    │    tanh(C(t)) × o(t)              │──→ h(t)
                    └───────────────────────────────────┘
```

### الـ Gates — 3 بوابات:

#### 1. Forget Gate — بوابة النسيان:
**بتشوف هل المعلومة اللي جاية من الماضي لسه مهمة ولا نرميها.**

```
f(t) = σ(W_f · [h(t-1), x(t)] + b_f)
```

- الـ output بين **0 و 1** لكل عنصر في الـ cell state
- **0** = انسى تماماً / **1** = احتفظ بيها كلها

#### 2. Input Gate — بوابة الإدخال:
**بتشوف المعلومة الجديدة مفيدة؟ لو أه، نحطها في الذاكرة.**

```
i(t) = σ(W_i · [h(t-1), x(t)] + b_i)        ← ايه اللي نضيفه؟
C̃(t) = tanh(W_C · [h(t-1), x(t)] + b_C)     ← القيمة الجديدة المقترحة
```

#### 3. تحديث الـ Cell State (الذاكرة):
**بنكوّن ذاكرة جديدة من المدخل الجديد + الحالة القديمة.**

```
C(t) = f(t) × C(t-1) + i(t) × C̃(t)
         ↑                ↑
    اللي نسيناه     اللي احتفظنا بيه
```

> الـ Final Memory بتكون ناتج الجمع بين اللي نسيناه واللي احتفظنا به.

#### 4. Output Gate — بوابة الخرج:
**بتحدد ايه جزء من الذاكرة هيتحوّل لـ hidden state يُستخدم لاحقاً.**

```
o(t) = σ(W_o · [h(t-1), x(t)] + b_o)
h(t) = o(t) × tanh(C(t))
```

---

## ملخص الـ LSTM بجدول

| الـ Gate | الوظيفة | المعادلة |
|---------|---------|----------|
| **Forget** | ايه اللي ننساه من الذاكرة القديمة | `f(t) = σ(W_f·[h,x] + b_f)` |
| **Input** | ايه اللي نضيفه من المعلومات الجديدة | `i(t) = σ(W_i·[h,x] + b_i)` |
| **Cell Update** | الذاكرة الجديدة | `C(t) = f·C(t-1) + i·C̃(t)` |
| **Output** | ايه اللي نطلعه كـ hidden state | `o(t) = σ(W_o·[h,x] + b_o)` |

---

## Gated Recurrent Unit (GRU)

الـ **GRU** أبسط من الـ LSTM — بيجمع بعض الـ gates مع بعض عشان يبقى **أسرع** مع أداء مشابه.

### الفروقات عن LSTM:

| الخاصية | LSTM | GRU |
|---------|------|-----|
| **عدد الـ Gates** | 3 (Forget, Input, Output) | **2** (Reset, Update) |
| **الـ Cell State** | منفصل عن الـ hidden state | **مفيش** — الـ hidden state بس |
| **التعقيد** | أعقد | **أبسط** |
| **سرعة التدريب** | أبطأ | **أسرع** |
| **الأداء** | أفضل على بيانات كتير | **مشابه** غالباً |

### الـ Gates:

#### 1. Reset Gate:
**بتتحكم في قد ايه من المعلومات السابقة نتجاهلها.**

```
r(t) = σ(W_r · [h(t-1), x(t)] + b_r)
```

#### 2. Update Gate:
**بتتحكم في قد ايه من الحالة القديمة نحتفظ بيها وقد ايه من الجديدة نضيفها.**

```
z(t) = σ(W_z · [h(t-1), x(t)] + b_z)
```

#### 3. الـ Hidden State الجديد:

```
h̃(t) = tanh(W · [r(t) × h(t-1), x(t)] + b)    ← candidate
h(t) = (1 - z(t)) × h(t-1) + z(t) × h̃(t)       ← final
```

> يعني الـ GRU بيدمج Forget + Input في gate واحد (Update)، وبيدمج Output في Reset. ده بيخليه أبسط وأسرع مع أداء كويس.

---

## Bidirectional RNNs

الـ **Bidirectional RNN** بتشغّل **RNN في اتجاهين** — من الشمال لليمين ومن اليمين للشمال — عشان تاخد **context من الماضي والمستقبل**.

```
Forward:   x₁ → x₂ → x₃ → x₄
            ↓     ↓     ↓     ↓
           h⃗₁ → h⃗₂ → h⃗₃ → h⃗₄

Backward:  x₁ ← x₂ ← x₃ ← x₄
            ↓     ↓     ↓     ↓
           h⃐₁ ← h⃐₂ ← h⃐₃ ← h⃐₄

Output:   [h⃗₁,h⃐₁] [h⃗₂,h⃐₂] [h⃗₃,h⃐₃] [h⃗₄,h⃐₄]
```

### مثال — Named Entity Recognition:

```
"Pankaj lives in Munich"
   ↓       ↓      ↓      ↓
 Person    O      O    Location
```

> الـ Bidirectional RNN بتقرأ الجملة من الشمال واليمين لتمثيل أفضل. لكن محتاجة الـ **input sequence كلها** — مش مناسبة للـ real-time tasks.

---

## تطبيقات الـ RNN/LSTM/GRU

| التطبيق | الوصف | النوع |
|---------|-------|-------|
| **Language Modeling** | توقع الكلمة الجاية في جملة | Many-to-Many |
| **Machine Translation** | ترجمة من لغة للغة (Encoder-Decoder) | Many-to-Many |
| **Image Captioning** | CNN (encoder) + RNN (decoder) — صورة → وصف | One-to-Many |
| **Video-to-Text** | وصف الفيديو frame by frame | Many-to-Many |
| **Sentiment Analysis** | تحليل المشاعر — إيجابي ولا سلبي | Many-to-One |
| **POS Tagging** | تحديد أنواع الكلمات (اسم، فعل...) | Many-to-Many (synced) |
| **NER** | تحديد الكيانات (أسماء، أماكن...) | Many-to-Many (synced) |

---

## مقارنة — RNN vs LSTM vs GRU

| الخاصية | RNN | LSTM | GRU |
|---------|-----|------|-----|
| **الذاكرة** | قصيرة | **طويلة** | **طويلة** |
| **الـ Gates** | مفيش | 3 gates | 2 gates |
| **الـ Cell State** | مفيش | ✅ منفصل | ❌ hidden state بس |
| **التعقيد** | بسيط | معقد | متوسط |
| **Vanishing Gradient** | بيعاني | **بيحلها** | **بيحلها** |
| **سرعة التدريب** | الأسرع | أبطأ | أسرع من LSTM |

---

> **نهاية المحاضرة العاشرة**
