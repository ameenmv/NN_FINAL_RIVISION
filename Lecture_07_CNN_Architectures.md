# المحاضرة السابعة — CNN Architectures

> Neural Network Course — FCIS, Mansoura University
> Book Pages: 235–260

---

## تطور معماريات الـ CNN

```
LeNet-5 (1998) → AlexNet (2012) → ZFNet (2013) → VGG (2014) → GoogLeNet (2014) → ResNet (2015) → DenseNet (2017)
```

---

## LeNet-5 (1998)

أول شبكة CNN ناجحة — مصممة للتعرف على **الأرقام المكتوبة بخط اليد** (MNIST dataset).

### البنية:

```
Input (32×32)
    ↓
Conv (5×5, 6 maps)     → 28×28×6
    ↓
Subsampling (Avg Pool)  → 14×14×6
    ↓
Conv (5×5, 16 maps)    → 10×10×16
    ↓
Subsampling (Avg Pool)  → 5×5×16
    ↓
Fully Connected         → 120 → 84
    ↓
Output                  → 10 classes
```

| الخاصية | التفاصيل |
|---------|----------|
| **الـ Input** | صورة 32×32 (زي رقم مكتوب ورقي) |
| **الـ Pooling** | **Average Pooling** |
| **الـ Activation** | Sigmoid / Tanh |
| **الدقة** | ~**99.2%** على MNIST |

> بتاخد صورة 32×32 وبتمررها على فلتر 5×5 عشان تستخرج حاجات مهمة (زي الخطوط والمنحنيات). بعدين بتقلل حجم الصورة (average pooling). بتكرر لحد ما توصل لـ fully connected عشان تعرف الرقم.

---

## AlexNet (2012) — ثورة الـ CNN

اللي عمل **ثورة** في الـ CNN — أول نموذج كبير بيكسب الـ ImageNet.

| الخاصية | التفاصيل |
|---------|----------|
| **البنية** | **8 طبقات**، **60 مليون parameter** |
| **الـ Activation** | **ReLU** بدل Sigmoid ← التدريب أسرع |
| **الـ Regularization** | **Dropout** ← يمنع overfitting |
| **الـ Pooling** | **Max Pooling** بدل Average |
| **التدريب** | على **GPU** (كان وقتها حاجة جديدة) |
| **الـ Data** | **Data Augmentation** |
| **النتيجة** | **16.4%** top-5 error (مقابل 26.2% لغير CNN) |

---

## ZFNet (2013)

تحسين على AlexNet — كسب **ILSVRC 2013** بـ **11.7% error**.

| التغيير | التفاصيل |
|---------|----------|
| **حجم الفلتر في CONV1** | من **11×11** stride 4 → **7×7** stride 2 ← عشان ياخد تفاصيل أدق |
| **عدد الفلاتر** | زادوا في الطبقات العميقة (**512, 1024, 512**) |

> يعني غيّروا حجم الفلتر في أول طبقة من 11×11 لـ 7×7 عشان ياخد تفاصيل أدق، وزادوا عدد الفلاتر في الطبقات العميقة.

---

## VGG (2014) — VGG-16 / VGG-19

فكرته إنه يستخدم **فلاتر صغيرة 3×3 متكررة** بدل فلاتر كبيرة.

| الخاصية | التفاصيل |
|---------|----------|
| **الفلاتر** | **3×3** في كل حاجة |
| **VGG-16** | **13** conv + **3** FC = 16 طبقة (**138M parameter**) |
| **VGG-19** | أعمق شوية — **19 طبقة** |
| **البنية** | **Uniform** — نفس النمط متكرر |
| **المشكلة** | الـ parameters كتير (**138 مليون**) فبياخد وقت كبير في التدريب |

### ليه 3×3 بدل 5×5؟

```
فلتر 5×5 واحد = 25 parameter
فلترين 3×3     = 18 parameter   ← أقل parameters بس نفس الـ receptive field!
```

> بدل ما تستخدم فلتر كبير (5×5 أو 7×7)، بيستخدم فلاتر صغيرة 3×3 متكررة — ده بيقلل الـ parameters بس نفس الأداء.

---

## GoogLeNet / Inception v1 (2014)

كسب **ILSVRC 2014** بـ **6.7% error** — مع إنه **أصغر من AlexNet بكتير** (5M parameters بس).

### الفكرة الأساسية — Inception Module:

بدل ما تختار بين 1×1 أو 3×3 أو 5×5، النموذج **بيستخدمهم كلهم مع بعض في نفس الطبقة!**

```
              Input
         ┌──────┼──────┬──────┐
         ↓      ↓      ↓      ↓
      1×1 Conv  3×3   5×5   Max
                Conv  Conv  Pool
         ↓      ↓      ↓      ↓
         └──────┴──────┴──────┘
              Concatenate
                  ↓
              Output
```

| الخاصية | التفاصيل |
|---------|----------|
| **الطبقات** | **22 طبقة** |
| **الـ Parameters** | **5 مليون** بس (مقابل 60M في AlexNet) |
| **1×1 Convolution** | بتستخدم **قبل** الفلاتر الكبيرة عشان تقلل عدد العمليات الحسابية (dimensionality reduction) |
| **النتيجة** | **6.7%** error |

### Auxiliary Classifiers:

GoogLeNet كان **عميق أوي** (22 طبقة) فكان بيواجه مشكلة الـ **Vanishing Gradient** — التعديلات مش بتبقى واضحة في أول الشبكة.

**الحل**: زوّدوا **"مدرسين مساعدين" (Auxiliary Classifiers)** في نص الشبكة — طبقات تصنيف (Softmax + Loss) مش بس في الآخر.

> يعني مثلاً بعد الطبقة 10 يبقى فيه تصنيف مؤقت بيدرب نفسه على الـ data. نفس الكلام بعد الطبقة 15.

### تطور الـ Inception:

| الإصدار | التحسينات |
|---------|-----------|
| **v2** | أدخل **Batch Normalization** + بدل 5×5 بفلترين 3×3 (أقل parameters) |
| **v3** | قسّم الفلاتر أكتر (3×3 → 1×3 + 3×1) — كفاءة أعلى |
| **v4** | بنية أبسط + أضاف **ResNet skip connections** |

---

## ResNet (2015) — كسر حاجز العمق

كسب **ILSVRC 2015** بـ **3.57% error** مع **152 طبقة**!

### المشكلة:
كل ما تزيد عدد الطبقات، الـ **Gradient بيفضل يضعف** (Vanishing Gradient) فالتدريب بيبقى صعب.

### الحل — Skip Connections (Residual Blocks):

```
       Input (x)
         ↓
    ┌─────────┐
    │  Conv    │
    │  BN+ReLU│
    │  Conv    │
    │  BN      │
    └────┬────┘
         │ F(x)
         ↓
    F(x) + x      ← هنا بيجمع الـ output مع الـ input الأصلي!
         ↓
       ReLU
         ↓
       Output
```

> يعني بدل ما الشبكة تتعلم الـ output مباشرة `H(x)`، بتتعلم الـ **الفرق** بس `F(x) = H(x) - x`. وده بيخلي الـ gradient يعدّي بسهولة حتى في الشبكات العميقة جداً.

---

## DenseNet (2017) — CVPR Best Paper

### الفكرة:
في **ResNet** كل طبقة بتتصل بالطبقة اللي **بعدها** بس. لكن في **DenseNet** كل طبقة بتتصل بـ **كل الطبقات اللي قبلها!**

```
ResNet:    L1 → L2 → L3 → L4
DenseNet:  L1 → L2 → L3 → L4
            └───→ L3 → L4
            └────────→ L4
                L2 ───→ L4
```

| الخاصية | التفاصيل |
|---------|----------|
| **الاتصال** | كل طبقة بتتصل بكل اللي قبلها (**concatenation**) |
| **تقليل Parameters** | بيستخدم **1×1 conv (Bottleneck Layer)** قبل كل 3×3 |
| **الميزة** | المعلومات بتتدفق بسهولة وبتقلل الضياع |

---

## Xception — Extreme Inception

### الفكرة:
بدل ما تعمل Convolution عادية، **بتقسمها لـ Depthwise + Pointwise**.

| النوع | الوصف |
|-------|-------|
| **Depthwise Conv** | بتشتغل على **كل channel لوحدها** |
| **Pointwise Conv (1×1)** | بتجمع **كل الـ channels مع بعض** |

> ده بيوفر وقت تدريب كبير مع أداء أفضل. ومستوحى من ResNet — فيه skip connections كمان.

---

## مقارنة شاملة

| المعمارية | السنة | الطبقات | Parameters | Error Rate | الابتكار |
|-----------|-------|---------|------------|------------|----------|
| **LeNet-5** | 1998 | 7 | 60K | — | أول CNN ناجح |
| **AlexNet** | 2012 | 8 | 60M | 16.4% | ReLU, Dropout, GPU |
| **ZFNet** | 2013 | 8 | — | 11.7% | فلاتر أصغر |
| **VGG-16** | 2014 | 16 | 138M | 7.3% | 3×3 فلاتر متكررة |
| **GoogLeNet** | 2014 | 22 | 5M | 6.7% | Inception Module |
| **ResNet** | 2015 | 152 | — | 3.57% | Skip Connections |
| **DenseNet** | 2017 | — | — | — | Dense Blocks |

---

> **نهاية المحاضرة السابعة**
